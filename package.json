{
  "name": "inventory-intelligence-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "packageManager": "pnpm@8.15.4",
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
